/*const city = '';
const enterCityName = (state=city, action)=>{
    switch(action.type)
    {
        case "CITYCHANGE":
        return city = e.target.value;
    }
}

export default enterCityName;*/